apiVersion: v1
kind: Service
metadata:
  name: {{ include "forsage-busd-frontend.fullname" . }}
  labels:
    {{- include "forsage-busd-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forsage-busd-frontend.selectorLabels" . | nindent 4 }}
